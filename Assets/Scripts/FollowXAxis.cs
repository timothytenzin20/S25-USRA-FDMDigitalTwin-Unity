using UnityEngine;

public class FollowXAxis : MonoBehaviour
{
    //    public Rigidbody X_axis;
    //    public Rigidbody printerHead;
    //    float yOffset = 0.1009f;

    //    void FixedUpdate()
    //    {
    //        // Only apply Y-following if the current movement command is for the beam (index 2)
    //        var command = ParseGCode.instance?.currentCommand;
    //        if (command != null && command.Value.rbIndex == 2) // beam
    //        {
    //            Vector3 headPos = printerHead.position;
    //            headPos.y = X_axis.position.y + yOffset;
    //            printerHead.MovePosition(headPos);
    //        }
    //    }
}
